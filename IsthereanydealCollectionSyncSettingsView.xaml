<UserControl x:Class="IsthereanydealCollectionSync.IsthereanydealCollectionSyncSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:IsthereanydealCollectionSync"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <UserControl.Resources>
        <local:BoolInverterConverter x:Key="BoolInverterConverter_ValueConverter"/>
        <sys:Boolean x:Key="True">True</sys:Boolean>
        <sys:Boolean x:Key="False">False</sys:Boolean>
    </UserControl.Resources>
    <StackPanel Margin="30,0,30,0" VerticalAlignment="Stretch">
        <StackPanel.Resources>
            <Style TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlockStyle}">
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Margin" Value="0,5,5,5" />
            </Style>
            <Style TargetType="RadioButton" BasedOn="{StaticResource {x:Type RadioButton}}">
                <Setter Property="Margin" Value="0,5,0,5" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>
            <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
                <Setter Property="Margin" Value="0,5,0,5" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>
        </StackPanel.Resources>
        <StackPanel Orientation="Horizontal" Margin="0,10,0,10">
            <Button Content="Authenticate" HorizontalAlignment="Left" Command="{Binding LoginCommand}" Margin="0,5,5,5"/>
            <TextBlock Margin="10,5,5,5" Text="{Binding AuthenticationStatus, IsAsync=True}" />
        </StackPanel>
        <TextBlock Text="Import Category"/>
        <StackPanel Orientation="Horizontal">
            <RadioButton Content="Create new category:" />
            <TextBox Text="{Binding Settings.ImportGroup}" Width="120" VerticalAlignment="Center" Margin="12,0,0,0"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <RadioButton Content="Use existing:" />
            <ComboBox ItemsSource="{Binding Categories}" 
                      IsReadOnly="True"
                      IsEditable="False"
                      SelectedIndex="0"
                      DisplayMemberPath="title"
                      SelectedValuePath="id"
                      SelectedValue="{Binding SelectedCategoryId}"/>
        </StackPanel>
        <TextBlock Text="Games already in Collection" Margin="0,20,0,0"/>
        <RadioButton Content="Ignore" IsChecked="{Binding Settings.ImportModeReplace, Converter={StaticResource BoolInverterConverter_ValueConverter}}"/>
        <RadioButton Content="Replace games and all their copies" IsChecked="{Binding Settings.ImportModeReplace}"/>
        <CheckBox Content="Remove imported games from Waitlist" IsChecked="{Binding Settings.RemoveFromWaitlist}"/>
        <TextBlock Text="{Binding PluginPath}" />
    </StackPanel>
</UserControl>